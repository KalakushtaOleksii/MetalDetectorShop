function ProductDto(id, name, image, category, trademark, country, frequencies, price ){
    return {
        id,
        name,
        image,
        category,
        trademark,
        country,
        frequencies,
        price,
    }
}

module.exports = ProductDto