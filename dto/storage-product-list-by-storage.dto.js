function StorageProductListByStorageDto(id, name, frequencies, quantity, price){
    return {
        id,
        name,
        frequencies,
        quantity,
        price,
    }
}

module.exports = StorageProductListByStorageDto